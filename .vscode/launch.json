{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Run C++ (MSYS2)",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe"
        }
    ]
}
